# Project instructions

## GitHub publishing

- Do not require or use the GitHub CLI (`gh`) for this repository.
- Use native `git` for initialization, commits, and pushes.
- If a pull request is needed, use an available GitHub connector; otherwise push without creating one.
