# QA report - 2026-08-11

## Automated and browser checks

- Node regression suite passed.
- Site audit passed for 8 sitemap URLs: unique title, description and H1; matching HTTPS/www canonical and Open Graph URL; valid internal links; parseable JSON-LD.
- Batch Code 128 ZIP contained three 615×265 RGBA PNG files. ZXing-C++ decoded each file to the expected value.
- PDF417 first PNG measured 561×93 RGBA and decoded to `SHIP-2026-0001`.
- Direct jsPDF export measured exactly 210×297 mm, rendered without clipping, and all three labels decoded from the rendered page.
- Microsoft Edge 151 rendered the desktop app correctly. Its print-to-PDF output measured 209.89×297.01 mm and matched the preview without clipping.
- Google Chrome 150 rendered the desktop app correctly. After `bwip-js 4.5.1` was moved from a runtime CDN to a local pinned asset, print-to-PDF also measured 209.89×297.01 mm and matched the preview.
- At 200 DPI, ZXing-C++ decoded all three expected Code 128 values from both the Chrome and Edge printed PDFs.
- A 390×844 mobile viewport had no horizontal page overflow. Navigation remained reachable by horizontal scrolling; controls and export buttons stacked in one column.

## Analytics checks

- Manual empty input produced `generate_result(error, empty_input)`; valid input produced `generate_result(success)` and `layout_commit`.
- CSV upload produced `import_result(success)` and exactly one `mapping_complete` on the first successful generation.
- Direct PDF download produced `export_result(success, pdf)` with barcode type, count bucket, A4 preset and page count.
- The PDF417 home page produced matching generate error/success and PNG export result events.
- Browser `dataLayer` contained no tested barcode value, file-name key, worksheet-name key or column-name key.
- No browser console warnings or errors were recorded during the analytics checks.

## Print CSS

Print media hides navigation, controls, download buttons and SEO copy, keeps only label pages, selects 210×297 mm or 8.5×11 in paper dimensions, avoids breaking cards, and adds a page break after every label page except the last.

## External acceptance still required

- Print ten labels on an office printer and ten on a thermal printer, then scan every label with the actual production scanner.
- Repeat URL Inspection and Rich Results Test after the updated files are deployed.
